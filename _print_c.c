#include "main.h"
/**
 * _print_c - a function that prints a character passed.
 * @c: the character passed to the function to be printed.
 *
 * Return: return the length of the character.
 */
int _print_c(char c)
{
	if (c)
		_putchar(c);
	return (1);
}
